import 'package:flutter/material.dart';

class CarouselAdvertisements extends StatelessWidget {
  const CarouselAdvertisements({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Carousel Advertisements'),
        titleSpacing: 0,
      ),
    );
  }
}
